version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: pharmacy-db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: branch_inventory
    ports:
      - "5432:5432"
